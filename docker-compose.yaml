x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "5"

services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER} -d $${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - internal
    logging: *default-logging

  redis:
    image: redis:7-alpine
    restart: unless-stopped
    env_file:
      - .env
    command: >
      sh -c 'redis-server
      --appendonly yes
      --requirepass "$${REDIS_PASSWORD}"'
    volumes:
      - ./data/redis:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a \"$${REDIS_PASSWORD}\" ping | grep PONG"]
      interval: 10s
      timeout: 5s
      retries: 10
    networks:
      - internal
    logging: *default-logging

  mid-radius-helper:
    image: ghcr.io/griefersutherland/mid-radius-helper:latest
    restart: unless-stopped
    env_file:
      - .env
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    volumes:
      - ./logs:/logs
      - ./data/helper:/data
      - ./config:/config:ro
    networks:
      - internal
    logging: *default-logging

  freeradius:
    image: freeradius/freeradius-server:latest
    restart: unless-stopped
    entrypoint: ["/bin/sh", "-c"]
    command:
      - |
        set -e
        apt-get update -qq
        apt-get install -y -qq --no-install-recommends curl ca-certificates >/dev/null
        rm -rf /var/lib/apt/lists/*
        exec sh /usr/local/bin/start-radius.sh
    env_file:
      - .env
    depends_on:
      - mid-radius-helper
    ports:
      - "1812:1812/udp"
      - "1813:1813/udp"
      # Always published - inert unless RADSEC_ENABLED=true (nothing listens
      # on it otherwise, so connection attempts just get refused).
      - "${RADSEC_PORT:-2083}:${RADSEC_PORT:-2083}/tcp"
    volumes:
      - ./scripts/start-radius.sh:/usr/local/bin/start-radius.sh:ro
      - ./scripts/verify-client-cert.sh:/usr/local/bin/verify-client-cert.sh:ro
      - ./scripts/check-policy.sh:/usr/local/bin/check-policy.sh:ro
      - ./certs:/etc/freeradius/certs:ro
      - ./logs:/logs
    networks:
      - internal
    logging: *default-logging

networks:
  internal:
